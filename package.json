{
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/",
    "dev": "live-server --port=4321 --no-browser --quiet",
    "start": "live-server dist --port=4321 --no-browser --quiet"
  },
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}

